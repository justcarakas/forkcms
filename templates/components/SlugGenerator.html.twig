{# No visible markup by design - see SlugGenerator and slug_generator_controller.js. #}
<div{{ attributes.defaults({
  'data-controller': 'backend--slug-generator',
  'data-backend--slug-generator-slug-value': slug,
}) }}></div>
