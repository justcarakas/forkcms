imports:
    - { resource: base_config.yaml }
    - { resource: parameters.yaml }

parameters:
    fork.is_installed: true

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    templating:
        class: Frontend\Core\Engine\TwigTemplate

    translator:
        class: Common\Language
